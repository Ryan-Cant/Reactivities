$kind: http-request
url: https://graph.facebook.com/me?access_token={{FACEBOOK_ACCESS_TOKEN}}&fields=name,email,picture.width(100).height(100)
method: GET
queryParams:
  access_token: "{{FACEBOOK_ACCESS_TOKEN}}"
  fields: name,email,picture.width(100).height(100)
order: 11000
